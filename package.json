{
  "name": "am-studio-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --check public/pipeline-admin.js && node --check public/episode-intake.js && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.28.1"
  }
}
